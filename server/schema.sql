
CREATE TABLE items (
    id INT AUTO_INCREMENT,
    name varchar(255),
    description varchar(255),
    price INT,
    discount INT,
    inventory INT,
    image varchar(255),
    PRIMARY KEY (id)

)